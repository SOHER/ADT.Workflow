@model ADT.Workflow.Web.Models.THEME

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>THEME</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.THEME_LABEL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.THEME_LABEL)
            @Html.ValidationMessageFor(model => model.THEME_LABEL)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
