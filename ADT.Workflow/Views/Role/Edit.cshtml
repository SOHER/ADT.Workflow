@model ADT.Workflow.Web.Models.ROLE

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ROLE</legend>

        @Html.HiddenFor(model => model.ROLE_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.ROLE_LABEL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ROLE_LABEL)
            @Html.ValidationMessageFor(model => model.ROLE_LABEL)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
