@model ADT.Workflow.Web.Models.FILE_TYPE

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FILE_TYPE</legend>

        @Html.HiddenFor(model => model.FILE_TYPE_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.FILE_TYPE_LABEL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FILE_TYPE_LABEL)
            @Html.ValidationMessageFor(model => model.FILE_TYPE_LABEL)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
