@model IEnumerable<ADT.Workflow.Web.Models.TEAM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TEAM_LABEL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TEAM_LABEL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TEAM_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TEAM_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TEAM_ID })
        </td>
    </tr>
}

</table>
