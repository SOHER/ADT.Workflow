@model ADT.Workflow.Web.Models.TEAM

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TEAM</legend>

        @Html.HiddenFor(model => model.TEAM_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.TEAM_LABEL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TEAM_LABEL)
            @Html.ValidationMessageFor(model => model.TEAM_LABEL)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
